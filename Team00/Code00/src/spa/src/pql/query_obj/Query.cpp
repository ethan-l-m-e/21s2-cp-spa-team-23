//
// Created by リム・イーサン on 30/1/22.
//

#include "Query.h"

std::string Query::generateResult(PKB *pkb) {
    // suchThatResults = such-that-clause.query(pkb) return if empty
    // patternResults = pattern-clause.query(pkb) return if empty

    // intersect results
    std::string result;
    return result;
}
