//
// Created by Tianyi Wang on 2/2/22.
//

#ifndef SPA_UTILITY_H
#define SPA_UTILITY_H


#include "Query.h"

class Utility {
    /*
    static DesignEntity findEntityType(std::string synonym, unordered_map<string, DesignEntity> declarations) {
        auto got = declarations.find (synonym);
        if ( got == declarations.end() )
            return NULL;
        else
            return got->second;
        return 0;
    }
    */
};


#endif //SPA_UTILITY_H
